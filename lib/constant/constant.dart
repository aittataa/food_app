import 'package:flutter/material.dart';

class Constant {
  static const appTitle = "TALABATAK";
  static const appDesc = "Food Delivery App";

  static const int itemCount = 25;
  static const Duration duration = Duration(milliseconds: 1500);
  static const Curve curve = Curves.linearToEaseOut;

  static const boxShadow = BoxShadow(
    color: Colors.black12,
    blurRadius: 10,
    spreadRadius: -5,
  );
}

final Color mainColor = Color(0xFFFEC915);
final Color backColor = Color(0xFFF0F0F0);
